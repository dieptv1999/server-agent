[Unit]
Description=Server Agent Monitor
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/server-agent -env /etc/server-agent/.env
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
